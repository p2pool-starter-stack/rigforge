# Per-host bench baselines for the e2e-real perf phase — see tests/README.md › Performance testing.
# One <hostname>.json per rig: {"bench_1m_hs": <H/s>, "cpu": "...", "recorded": "YYYY-MM-DD"}.
# Record/update with: E2E_PERF_RECORD=1 sudo bash tests/e2e-real.sh perf   (then commit the file)
